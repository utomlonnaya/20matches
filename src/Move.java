public class Move {
    boolean isUserMove = false;

    public boolean getMove() {
        return isUserMove;
    }

    public void setUserMove(boolean userMove) {
        isUserMove = userMove;
    }
}